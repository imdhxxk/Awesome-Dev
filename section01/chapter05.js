// 1. Number Type
let num1 = 27;
let num2 = 1.5;
let num3 = -20;

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN; 

// 2. String Type
let myName = "임동혁";
let myLocation = "시흥시 정왕동";
let introduce =  myLocation + "에 사는 " + myName + "입니다.";
// 자바스크림트 문자열 덧셈 연산 지원
let introduceText = `${myName}은 ${myLocation}에 거주합니다.`;
// `은 템플릿 리터럴 문법

// 3. Boolean Type
let isSwitchOn = true;
let isEmpty = false;

// 4. Null Type (아무것도 없다)
let empty = null; //개발자 들이 직접 값이 없음을 명시적으로 표현하기 위해 사용

// 5. Undefined Type (값이 할당되지 않은 상태)
let none; // 변수 선언 후 값을 할당하지 않으면 undefined가 기본값으로 할당된다.

